@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
<div class=" container ">
    <div class="row">
        <div class="col-lg-12">
            <!--begin::Card-->
            <div class="card card-custom">
                <div class="card-header flex-wrap border-0 pt-6 pb-0">
                    <h3 class="card-title">
                        Nossos Dizimistas
                    </h3>

                    <div class="card-toolbar">
                        <a href="@Url.Action("AddDizimista", "Dizimista")"><i class="fas fa-plus"></i> Adicionar</a>
                    </div>

                </div>

                <div class="card-body">
                    <div class="datatable datatable-bordered" id="kt_datatable" style=""></div>
                </div>
            </div>



            <!--end::Card-->
        </div>
    </div>
</div>
@section Scripts {

    <script src="~/js/Dizimista/GetAllDizimista.js"></script>

}










@* @model IEnumerable<SouDizimista.Services.ServicesEntity.ServiceDizimista>

@{
    ViewData["Title"] = "Nossos Dizímistas";
    Layout = "_Layout"; // Especifica o layout principal
}

<h3 class="color-source" ">Nossos Dizímistas</h3>
<hr>

<table >
    <thead>
        <tr>
            <th class="text-source">
                @Html.DisplayName("Nome")
            </th class="text-source">

            <th class="text-source">
                @Html.DisplayName("Valor")
            </th class="text-source">

            <th class="text-source">
                @Html.DisplayName("Opções")
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr >
            <td class="text-source">
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td class="text-source">
                @Html.DisplayFor(modelItem => item.ValorDevolucao)
            </td>

            <td>
                @Html.ActionLink("Alterar", "ObterDizimistas", new { id = item.Id }, new { @class = "btn btn-info" })
                @Html.ActionLink("Consultar", "ConsultDizimista", new { id = item.Id }, new {@class = "btn btn-secondary" })
                @Html.ActionLink("Excluir", "Delete", new { id = item.Id }, new { @class = "btn btn-danger" })
            </td>
        </tr>
        }
    </tbody>
</table>
 *@